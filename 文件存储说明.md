# 文件存储配置说明

## 当前实现方式

当前系统支持两种文件存储方式：

### 1. 本地文件存储（默认）

文件保存在服务器的 `uploads` 目录下，通过 `/api/files/**` 接口访问。

**配置方式：**
- 在 `application.yml` 中，`FILE_ACCESS_BASE_URL` 留空或不配置
- 文件访问URL格式：`/api/files/avatar/{userId}/{filename}`

**优点：**
- 实现简单，无需额外服务
- 适合小规模应用

**缺点：**
- 文件存储在服务器本地，扩容不便
- 服务器重启或迁移可能导致文件丢失
- 不适合分布式部署

### 2. 使用腾讯云 COS（对象存储）

如果需要使用腾讯云 COS 存储文件，需要：

1. **开通腾讯云 COS 服务**
   - 登录腾讯云控制台
   - 创建 COS 存储桶
   - 获取 SecretId、SecretKey 和存储桶名称

2. **配置环境变量**
   ```
   FILE_ACCESS_BASE_URL=https://your-bucket.cos.ap-guangzhou.myqcloud.com
   ```

3. **集成 COS SDK**（需要修改代码）
   - 添加腾讯云 COS SDK 依赖
   - 修改 `FileServiceImpl` 使用 COS 上传文件
   - 文件上传后直接返回 COS 的访问URL

## FILE_ACCESS_BASE_URL 配置说明

### 本地存储模式（默认）
```yaml
# application.yml 中不配置或留空
file:
  access:
    base-url: ${FILE_ACCESS_BASE_URL:}
```

文件访问URL：`/api/files/avatar/1/xxx.jpg`
完整URL：`http://your-domain.com/api/files/avatar/1/xxx.jpg`

### 使用外部文件服务器
如果使用 CDN 或外部文件服务器，配置基础URL：

```yaml
file:
  access:
    base-url: ${FILE_ACCESS_BASE_URL:https://cdn.example.com}
```

文件访问URL：`https://cdn.example.com/avatar/1/xxx.jpg`

### 使用腾讯云 COS
```yaml
file:
  access:
    base-url: ${FILE_ACCESS_BASE_URL:https://your-bucket-1234567890.cos.ap-guangzhou.myqcloud.com}
```

文件访问URL：`https://your-bucket-1234567890.cos.ap-guangzhou.myqcloud.com/avatar/1/xxx.jpg`

## 推荐方案

### 开发/测试环境
使用本地文件存储（当前实现）

### 生产环境
推荐使用腾讯云 COS：
1. 高可用性和可扩展性
2. 支持 CDN 加速
3. 自动备份和容灾
4. 按量付费，成本可控

## 集成腾讯云 COS 的步骤

如果需要集成 COS，需要：

1. **添加依赖**（在 pom.xml 中）：
```xml
<dependency>
    <groupId>com.qcloud</groupId>
    <artifactId>cos_api</artifactId>
    <version>5.6.89</version>
</dependency>
```

2. **修改 FileServiceImpl**，使用 COS SDK 上传文件

3. **配置环境变量**：
   - `COS_SECRET_ID`: 腾讯云 SecretId
   - `COS_SECRET_KEY`: 腾讯云 SecretKey
   - `COS_REGION`: COS 地域（如：ap-guangzhou）
   - `COS_BUCKET_NAME`: 存储桶名称
   - `FILE_ACCESS_BASE_URL`: COS 访问域名

## 注意事项

1. **文件大小限制**：当前配置为 5MB，可在 `application.yml` 中修改
2. **文件类型限制**：仅支持图片格式（jpg, jpeg, png, gif, webp）
3. **安全性**：本地存储模式下，确保 `uploads` 目录有适当的访问权限
4. **备份**：使用本地存储时，建议定期备份 `uploads` 目录

